<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EXAM</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap');

        * {
            margin: 0;
            padding: 0;
            scrollbar-width: none;
            box-sizing: border-box;
            outline: none;
            font-family: Poppins;
        }

        .container {
            width: 100%;
            height: 100vh;
        }

        .navbar{
            width: 100%;
            height: 60px;
            background-color: lightblue;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 5%;

            a{
                padding: 10px 30px;
                text-decoration: none;
                color: black;
                font-weight: 700;
            }

            a:hover{
                background-color: rebeccapurple;
                transition: .15s ease-in-out;
                color: white;
            }
        }

        .exam{
            width: 100%;
            height: calc(100% - 60px);
        }
        ol{
            width: 100%;
            text-transform: uppercase;
            font-style: italic;
            font-weight: 700;
        }
        .form{
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 25px;
            padding: 40px 0;
            input{
                width: 100%;
                padding: 10px 20px;
            }
        }
        .box{
            display: flex;
            flex-direction: column;
            gap: 15px;
            padding: 10px 10%;
        }

        button{
            padding: 15px 40px;
            width: max-content;
            background-color: black;
            color: white;
            border: none;
        }

        button:hover{
            background-color: green;
            transition: .15s ease-in-out;
        }
    </style>
</head>

<body>
    <div class="container">

        <div class="navbar">
            <a href="#" style="text-transform: uppercase;">CURRENT USER : <%= user %></a>
            <a href="/logout">LOGOUT</a>
        </div>

        <div class="exam">
            <form action="/exam" class="form" method="post">
                <ol>
                    <% questions.forEach((i)=>{ %>
                        <div class="box">
                             <li><%= i.question %></li>
                             <input type="text" name="<%= i.id %>" placeholder="enter your answer for <%= i.question %>" required>
                        </div>
                     <% }) %>
                </ol>
                <button>SUBMIT ANSWERS</button>
            </form>
        </div>
    </div>
</body>

</html>